//async function getPaths(context) {
//  const { albumsID } = await albumGql(context, context.query.id);
//  return {
//    paths: albumsID.map((album) => ({
//      params: {
//        id: album.id,
//      },
//    })),
//    fallback: false,
//  };
//}
